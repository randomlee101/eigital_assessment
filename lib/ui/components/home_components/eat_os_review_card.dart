import 'package:flutter/material.dart';

class EatOsReviewCard extends StatelessWidget {
  const EatOsReviewCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        CircleAvatar(),
        SizedBox(),
        Text("data")
      ],
    );
  }
}
